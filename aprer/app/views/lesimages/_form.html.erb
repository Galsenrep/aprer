<%= form_for @lesimage, html: { multipart: true } do |form| %>
<% if @lesimage.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@lesimage.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
    <% @lesimage.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="col-md-6">
    <div class="form-group">
    <label for="">Titre de l'image:</label>
    <%= form.text_field :titre, class:'form-control' %>
  </div>

  <div class="form-group">
    <label for="">Charger votre image:</label>
    <%= form.file_field :image %>
  </div>

  <div class="form-group">
    <button type="submit" class="btn btn-success"> Enregistrer </button>
    <a href="<%= lesimages_path %>" class="btn btn-danger">Annuler</a>
  </div>
</div>

<% end %>
